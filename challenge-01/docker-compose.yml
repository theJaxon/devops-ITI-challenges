version: "3.8"
services:
  tornado:
    build: .
    container_name: python_iti_challenge_test
    command: ["hello.py"]

    environment: 
      - REDIS_HOST=redis 
      - REDIS_PORT=6379
      - REDIS_DB=0
      - ENVIRONMENT=Test 
      - HOST=localhost
      - PORT=4111
    depends_on: 
      - redis
    ports: 
      - "4111:4111"

  redis:
    image: redis:alpine
    container_name: redis_iti_challenge_test
    expose:
      - 6379
    deploy:
      replicas: 2
      update_config:
        parallelism: 2
        delay: 10s
      restart_policy:
        condition: on-failure
        